export enum RouterPath {
  home = '/',
  services = '/services',
  projects = '/projects',
  about = '/about',
  carrers = '/carrers',
  contact = '/contact',
}

export enum GuestRouterPath {
  home = '/',
  logout = '/logout',
  signIn = '/sign-in',
}
